# Screenshots

`miku-score-web.png` is a Chromium capture of the generated offline
single-file app at a fixed 1440px viewport. Regenerate it after a deliberate
visual change:

```sh
npm run build
npm run screenshot
```

Historical screenshots in the upstream repository are not copied here because
they show unported measure-editor and custom-element UI flows.
